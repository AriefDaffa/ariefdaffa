---
import HomeLayout from '@/layouts/HomeLayout.astro'
import Scene from '@/components/Scene'
---

<HomeLayout title="Arief Daffa">
  <div class="h-screen flex flex-col">
    <div class="w-full md:h-1/4 flex border-b-[1px] dark:border-gray-800 flex-col md:flex-row">
      <div class="md:w-2/3 px-8 py-4 flex flex-col justify-between">
        <div class="md:w-1/2 text-xl ">
          A <span class="text-primary font-semibold">Web Developer</span> who loves learning new things that it&apos;s become an obsession—almost to the
              point of getting sick of them!
        </div>
        <div class="text-gray-500">© Arief Daffa 2024</div>
      </div>
      <div class="md:w-1/3 border-t-[1px] md:border-t-0 border-l-[1px] flex flex-col dark:border-gray-800">
        <div class="px-8 py-4 dark:border-gray-800 flex flex-col justify-between flex-1">
          <div class="text-gray-500">Navigate</div>
          <div class="self-end text-right flex flex-col gap-2">
            <a href="/projects" class="cursor-pointer hover:text-primary">Projects</a>
            <a href="/blog" class="cursor-pointer hover:text-primary">Blogs</a>
            <a href="/about" class="cursor-pointer hover:text-primary">About</a>
          </div>
        </div>
        <div class="px-8 py-4 flex justify-between border-t-[1px] dark:border-gray-800">
          <div class="text-gray-500">Dark Mode</div>
          <div class="">
            <button id="theme-toggle">
              <div class="dark:hidden">🌙</div>
              <div class="hidden dark:block">☀️</div>
            </button>
          </div>
        </div>
      </div>
    </div>
    <div class="w-full md:h-3/4">
      <Scene client:load  />
    </div>
  </div>
</HomeLayout>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const themeToggleButton = document.getElementById('theme-toggle')
    const htmlElement = document.documentElement
    const currentTheme = localStorage.getItem('theme')

    if (currentTheme === 'dark') {
      htmlElement.classList.add('dark')
    }

    themeToggleButton?.addEventListener('click', function () {
      htmlElement.classList.toggle('dark')
      let theme = 'light'
      if (htmlElement.classList.contains('dark')) {
        theme = 'dark'
      }
      localStorage.setItem('theme', theme)
    })
  })
</script>